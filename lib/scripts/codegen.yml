overwrite: true
schema: "./schemas/*.gql"
# documents: "./ops"
config:
  scalars:
    DateTime: Date
    AnyType: any
    URI: string
generates:
  index.d.ts:
    plugins:
      - "typescript"
  #build/graphql.tsx:
    #plugins:
      #- "typescript-react-apollo"
      #- "typescript-operations"
  #build/graphql.d.ts:
    #plugins:
      #- "typescript-graphql-files-modules"
